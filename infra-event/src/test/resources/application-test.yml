spring:
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
  cloud:
    nacos:
      discovery:
        enabled: false
infra:
  kafka:
    topic: test-topic
